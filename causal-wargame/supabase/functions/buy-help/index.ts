import { body, json, options } from '../_shared/http.ts';import { rpc } from '../_shared/rpc.ts'
Deno.serve(async req=>{const pre=options(req);if(pre)return pre;try{const token=req.headers.get('x-game-token')||'';const b=await body(req);if(!token)return json({error:'Falta x-game-token'},401);return json(await rpc('cw_buy_help',{p_token:token,p_help_id:b.help_id}))}catch(e){return json({error:e.message},400)}})
