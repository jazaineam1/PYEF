import React,{useEffect,useState}from'react';import{Check,Lock,Sparkles}from'lucide-react';import{Layout}from'../components/Layout';import{Timer}from'../components/Timer';import{Leaderboard}from'../components/Leaderboard';import{invoke,savePlayerToken}from'../lib/api';import{MICROCHECKS,ROLES,ROUND_COPY}from'../content';
function Join({onJoined}){const[code,setCode]=useState('');const[name,setName]=useState('');const[busy,setBusy]=useState(false);const[err,setErr]=useState('');async function go(){setBusy(true);setErr('');try{const r=await invoke('join-game',{game_code:code,display_name:name});savePlayerToken(r.token);sessionStorage.setItem('df_game_code',code.toUpperCase());onJoined()}catch(e){setErr(e.message)}finally{setBusy(false)}}return <Layout subtitle="Participante"><div className="grid grid-2"><div className="card card-accent"><div className="eyebrow">ENTRAR A LA PARTIDA</div><h2>Tu misión está por comenzar</h2><p>Tu equipo y rol se asignan automáticamente para mantener cuatro equipos de cinco personas.</p><div className="form-row"><label>Código de partida</label><input className="input" autoCapitalize="characters" value={code} onChange={e=>setCode(e.target.value.toUpperCase())}/></div><div className="form-row"><label>Nombre visible</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/></div>{err&&<div className="notice notice-red">{err}</div>}<button className="btn btn-primary" disabled={busy||code.length<4||name.trim().length<2} onClick={go}>{busy?'Entrando…':'Entrar'}</button></div><div className="card"><h3>Una regla</h3><p>Puedes usar IA. Pero una respuesta de IA no es evidencia: la información del juego está repartida entre tu equipo y el sistema libera datos nuevos en vivo.</p></div></div></Layout>}
function useRemoteState(enabled=true){const[state,setState]=useState(null);const[error,setError]=useState('');async function refresh(){try{setState(await invoke('game-state'));setError('')}catch(e){setError(e.message)}}useEffect(()=>{if(!enabled)return;refresh();const i=setInterval(refresh,3000);return()=>clearInterval(i)},[enabled]);return{state,error,refresh}}
function keyFor(round,team){const k=`df-idem-${round}-${team}`;let v=sessionStorage.getItem(k);if(!v){v=crypto.randomUUID();sessionStorage.setItem(k,v)}return v}
function Round1({state,onSubmit}){const existing=state.decision?.selected||[];const[selected,setSelected]=useState(existing);useEffect(()=>setSelected(existing),[JSON.stringify(existing)]);const customers=state.customers||[];const toggle=id=>setSelected(v=>v.includes(id)?v.filter(x=>x!==id):v.length<10?[...v,id]:v);return <><div className="notice notice-gold">ORÁCULO estima probabilidad de conversión. <b>Elige exactamente 10 clientes.</b> Nada en esta pantalla muestra todavía el “otro futuro”.</div><div className="customer-grid" style={{marginTop:14}}>{customers.map(c=><button type="button" className={`customer ${selected.includes(c.id)?'selected':''}`} key={c.id} onClick={()=>!state.locked&&toggle(c.id)}><div className="check">{selected.includes(c.id)&&<Check size={14}/>}</div><h4>{c.name}</h4><div className="seg">{c.segment}</div><div className="score-badge">{Math.round(Number(c.score)*100)}% <small>score</small></div></button>)}</div><div className="section-title"><p>{selected.length}/10 seleccionados</p><button className="btn btn-primary" disabled={state.locked||selected.length!==10} onClick={()=>onSubmit({selected})}><Lock size={16}/> {state.locked?'Decisión bloqueada':'Bloquear decisión'}</button></div></>}
function Round2({state,onSubmit}){const role=state.player.role_code;const[d,setD]=useState(()=>({recommendation:state.decision?.recommendation||'',confounder:state.decision?.confounder||'',reason:state.decision?.reason||''}));return <><div className="grid grid-2"><div className="card role-card"><div className="label">TU INFORMACIÓN · {ROLES.find(r=>r.code===role)?.label}</div><p className="intel">{state.role_card||'Tu tarjeta se liberará con la ronda.'}</p></div><div className="card"><div className="metric"><span>Clientes llamados</span><strong>20%</strong><small>pagan a 30 días</small></div><div className="metric" style={{marginTop:16}}><span>No llamados</span><strong>35%</strong><small>pagan a 30 días</small></div></div></div><div className="grid grid-2" style={{marginTop:14}}><div className="card"><h3>¿Qué recomiendan?</h3>{[['cancel','Cancelar'],['keep','Mantener tal cual'],['redesign','Rediseñar antes de concluir']].map(([v,l])=><label className="choice" key={v}><input type="radio" checked={d.recommendation===v} onChange={()=>setD({...d,recommendation:v})}/>{l}</label>)}</div><div className="card"><h3>¿Qué variable hace peligrosa la comparación?</h3><select className="select" value={d.confounder} onChange={e=>setD({...d,confounder:e.target.value})}><option value="">Selecciona…</option><option value="edad">Edad</option><option value="mora_previa">Mora previa</option><option value="nombre">Nombre</option></select><div className="form-row"><label>Explícalo sin jerga</label><textarea className="textarea" value={d.reason} onChange={e=>setD({...d,reason:e.target.value})}/></div></div></div><div className="section-title"><span/><button className="btn btn-primary" disabled={state.locked||!d.recommendation||!d.confounder||d.reason.length<12} onClick={()=>onSubmit(d)}>{state.locked?'Decisión bloqueada':'Bloquear decisión'}</button></div></>}
function Round3({state,onSubmit}){const[d,setD]=useState(()=>state.decision&&Object.keys(state.decision).length?state.decision:{assignment:'',outcome:'pago_30d',horizon:30});return <div className="grid grid-3">{[['advisor','El asesor decide','Usar criterio humano.'],['model','ORÁCULO decide','Tratar scores más altos.'],['random','Asignar al azar','Dividir elegibles entre intervención y control.']].map(([v,t,p])=><button className={`card ${d.assignment===v?'card-accent':''}`} key={v} onClick={()=>!state.locked&&setD({...d,assignment:v})}><h3>{t}</h3><p>{p}</p></button>)}<div className="card" style={{gridColumn:'1/-1'}}><div className="grid grid-2"><div className="form-row"><label>Outcome</label><select className="select" value={d.outcome} onChange={e=>setD({...d,outcome:e.target.value})}><option value="pago_30d">Pago a 30 días</option><option value="click">Click inmediato</option></select></div><div className="form-row"><label>Horizonte</label><select className="select" value={d.horizon} onChange={e=>setD({...d,horizon:Number(e.target.value)})}><option value="1">1 día</option><option value="30">30 días</option><option value="90">90 días</option></select></div></div><button className="btn btn-primary" disabled={state.locked||!d.assignment} onClick={()=>onSubmit(d)}>{state.locked?'Diseño bloqueado':'Bloquear diseño'}</button></div></div>}
function Round4({state,onSubmit}){const segments=state.segments||[];const[actions,setActions]=useState(()=>Object.fromEntries(segments.map(s=>[s.id,'observe'])));useEffect(()=>{if(segments.length&&!Object.keys(actions).length)setActions(Object.fromEntries(segments.map(s=>[s.id,'observe'])))},[segments.length]);function send(){onSubmit({treat:segments.filter(s=>actions[s.id]==='treat').map(s=>s.id),avoid:segments.filter(s=>actions[s.id]==='avoid').map(s=>s.id),observe:segments.filter(s=>actions[s.id]==='observe').map(s=>s.id)})}return <><div className="card">{segments.map(s=><div className="team-status" style={{gridTemplateColumns:'1.6fr .6fr 1.4fr'}} key={s.id}><div><b>{s.name}</b></div><div style={{color:Number(s.effect)<0?'var(--red)':Number(s.effect)>5?'var(--green)':'var(--muted)',fontWeight:900}}>{Number(s.effect)>0?'+':''}{s.effect} pp</div><div><select className="select" disabled={state.locked} value={actions[s.id]||'observe'} onChange={e=>setActions({...actions,[s.id]:e.target.value})}><option value="treat">Intervenir</option><option value="avoid">No intervenir</option><option value="observe">Más evidencia</option></select></div></div>)}</div><div className="section-title"><span/><button className="btn btn-primary" disabled={state.locked||!segments.length} onClick={send}>{state.locked?'Política bloqueada':'Bloquear política'}</button></div></>}
function Reveal({state}){const r=state.game.round;if(r===1){const rows=state.reveal_payload||[];const observed=rows.length?Math.round(100*rows.reduce((a,c)=>a+Number(c.p1),0)/rows.length):0;const impact=rows.length?Math.round(100*rows.reduce((a,c)=>a+Number(c.effect),0)):0;const best=[...rows].sort((a,b)=>Number(b.effect)-Number(a.effect))[0];return <><div className="reveal-stage"><div className="eyebrow">LO QUE OBSERVABAS</div><div className="giant">{observed}%</div><p>conversión esperada entre seleccionados.</p><div className="notice notice-gold" style={{maxWidth:760,margin:'24px auto'}}>Ahora el simulador puede enseñarte algo que en datos reales nunca observas directamente: el otro futuro.</div><div className="giant" style={{color:'var(--cyan)'}}>+{impact}</div><p>impacto incremental acumulado esperado.</p></div>{best&&<div className="split-future"><div className="future"><div className="eyebrow">INTERVENIMOS</div><h3>{best.name}</h3><strong>{Math.round(Number(best.p1)*100)}%</strong></div><div className="vs">VS</div><div className="future"><div className="eyebrow">NO INTERVENIMOS</div><h3>{best.name}</h3><strong>{Math.round(Number(best.p0)*100)}%</strong></div></div>}</>}
if(r===2)return <div className="reveal-stage"><div className="eyebrow">ASOCIACIÓN OBSERVADA</div><div className="giant" style={{color:'var(--red)'}}>{state.result?.naive_effect_pp??'−15'} pp</div><p>parecía que llamar empeoraba.</p><div style={{height:20}}/><div className="eyebrow">VERDAD DEL SIMULADOR</div><div className="giant" style={{color:'var(--green)'}}>+{state.result?.causal_effect_pp??5} pp</div><p>la selección estaba escondiendo el efecto.</p></div>
if(r===3)return <div className="reveal-stage"><div className="split-future"><div className="future"><h3>Intervención</h3><strong>{state.result?.treatment??31}%</strong></div><div className="vs">−</div><div className="future"><h3>Control</h3><strong>{state.result?.control??25}%</strong></div></div><div className="giant" style={{marginTop:24,color:'var(--cyan)'}}>+{state.result?.ate_pp??6} pp</div><p>Efecto promedio de la comparación aleatoria.</p></div>
return <div className="reveal-stage"><div className="eyebrow">EL PROMEDIO NO ES TODOS</div><div className="giant">+6 pp</div><p>La política final debe considerar a quién ayuda, a quién casi no mueve y a quién perjudica.</p></div>}
function Microcheck({state,refresh}){const q=MICROCHECKS[state.game.round];const[ans,setAns]=useState('');const[done,setDone]=useState(null);async function send(){const r=await invoke('submit-check',{round:state.game.round,answer:ans});setDone(r.correct);await refresh()}return <div className="card card-accent"><div className="eyebrow">MICROCHECK INDIVIDUAL</div><h2>{q.prompt}</h2>{q.options.map(o=><label className="choice" key={o}><input type="radio" checked={ans===o} onChange={()=>setAns(o)}/>{o}</label>)}{done!==null?<div className={`notice ${done?'notice-green':'notice-red'}`} style={{marginTop:14}}>{done?'Correcto.':'Revísalo en el debrief; el objetivo es aprender, no acertar a la primera.'}</div>:<button className="btn btn-primary" style={{marginTop:14}} disabled={!ans} onClick={send}>Enviar</button>}</div>}
function Game(){const{state,error,refresh}=useRemoteState(true);const[busy,setBusy]=useState(false);if(error&&!state)return <Layout subtitle="Participante"><div className="notice notice-red">{error}. Vuelve a ingresar si tu sesión expiró.</div></Layout>;if(!state)return <Layout subtitle="Participante"><div className="card">Conectando con la partida…</div></Layout>;const copy=ROUND_COPY[state.game.round];const team=state.teams.find(t=>t.id===state.player.team_id);async function submit(payload){setBusy(true);try{await invoke('submit-decision',{round:state.game.round,payload,idempotency_key:keyFor(state.game.round,state.player.team_id)});await refresh()}catch(e){alert(e.message)}finally{setBusy(false)}}return <Layout subtitle="Participante" actions={<><span className="pill">Equipo <strong>{team?.name}</strong></span><span className="pill">Rol <strong>{ROLES.find(r=>r.code===state.player.role_code)?.label}</strong></span></>}><div className="round-header"><div><div className="eyebrow">{copy.kicker}</div><h2>{copy.title}</h2><div className="mission">{copy.mission}</div></div><Timer closesAt={state.game.closes_at?new Date(state.game.closes_at).getTime():null}/></div>{error&&<div className="notice notice-red" style={{marginBottom:12}}>Reconexión: {error}</div>}{['lobby','briefing'].includes(state.game.phase)?<div className="card card-accent"><h2>Esperando al facilitador</h2><p>{state.game.public_message}. Comparte tu rol con tu equipo sólo cuando la ronda lo requiera.</p></div>:state.game.phase==='round'?<>{state.game.round===1&&<Round1 state={state} onSubmit={submit}/>} {state.game.round===2&&<Round2 state={state} onSubmit={submit}/>} {state.game.round===3&&<Round3 state={state} onSubmit={submit}/>} {state.game.round===4&&<Round4 state={state} onSubmit={submit}/>}</>:state.game.phase==='closed'?<div className="card"><h2>Decisiones cerradas</h2><p>Preparando revelación…</p></div>:state.game.phase==='reveal'?<><Reveal state={state}/><div className="card"><Leaderboard state={state}/></div></>:state.game.phase==='teaching'?<div className="card card-accent"><div className="eyebrow">CONCEPTO DESBLOQUEADO</div><h2>{copy.teaching.title}</h2><p style={{fontSize:20}}>{copy.teaching.body}</p></div>:state.game.phase==='microcheck'?<Microcheck state={state} refresh={refresh}/>:state.game.phase==='finished'?<><div className="reveal-stage"><Sparkles size={40}/><div className="giant">¿A quién podemos cambiar?</div><p>ORÁCULO predecía. El equipo aprendió a pensar causalmente.</p></div><div className="card"><Leaderboard state={state}/></div></>:<div className="card">Partida pausada.</div>}{busy&&<div className="pill" style={{position:'fixed',right:20,bottom:20}}>Guardando en servidor…</div>}</Layout>}
export default function SecurePlay(){const[token,setToken]=useState(()=>sessionStorage.getItem('df_game_token'));return token?<Game/>:<Join onJoined={()=>setToken(sessionStorage.getItem('df_game_token'))}/>}
