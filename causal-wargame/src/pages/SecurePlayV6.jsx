import React,{useEffect,useMemo,useState}from'react'
import{Check,Lock,LogOut,Sparkles}from'lucide-react'
import{Layout}from'../components/Layout'
import{Timer}from'../components/Timer'
import{Leaderboard}from'../components/Leaderboard'
import{LessonPanel}from'../components/LessonPanel'
import{RoleLab}from'../components/RoleLab'
import{invoke,savePlayerToken,savePlayerGameCode,getPlayerToken,clearPlayerSession}from'../lib/api'
import{MICROCHECKS,ROLES,ROUND_COPY}from'../content'

const sleepJitter=base=>Math.round(base*(.8+Math.random()*.4))
function roleLabel(code){return ROLES.find(r=>r.code===code)?.label||code}
function keyFor(round,team){const k=`df-idem-${round}-${team}`;let v=sessionStorage.getItem(k);if(!v){v=crypto.randomUUID();sessionStorage.setItem(k,v)}return v}

const TRANSFER={
pre:[
 ['q1','Un modelo predice con 92% de precisión quién pagará. ¿Eso identifica automáticamente a quién cambiará una llamada?',[['a','Sí, porque predice muy bien.'],['b','No. Predicción y efecto de intervenir son preguntas distintas.'],['c','Sí, si la muestra es grande.']]],
 ['q2','Los clientes llamados tenían más mora desde antes y también pagan menos. ¿Qué debes revisar primero?',[['a','Sólo el AUC del modelo.'],['b','El color del canal.'],['c','Si la mora previa afecta tanto recibir llamada como pagar.']]],
 ['q3','Puedes asignar una campaña entre clientes elegibles. ¿Qué diseño ayuda más a crear grupos comparables?',[['a','Que el asesor elija.'],['b','Asignación aleatoria.'],['c','Sólo tratar los scores más altos.']]],
 ['q4','El efecto promedio es +6 pp, pero algunos perfiles casi no cambian y otros empeoran. ¿Qué sigue?',[['a','Tratar a todos.'],['b','Ignorar segmentos.'],['c','Evaluar heterogeneidad, incertidumbre, costo y riesgo.']]]
],
post:[
 ['q1','Un banco encuentra clientes con altísimo riesgo de fuga. ¿Son necesariamente quienes más se benefician de una llamada de retención?',[['a','Sí, riesgo alto implica efecto alto.'],['b','No. Necesitamos estimar el cambio incremental causado por llamar.'],['c','Sí, si el modelo está calibrado.']]],
 ['q2','Quienes reciben asesoría financiera ahorran más, pero eran usuarios más activos desde antes. ¿Qué impide concluir causalidad directamente?',[['a','Que el resultado sea dinero.'],['b','Que haya muchos usuarios.'],['c','Los grupos pueden diferir antes de la asesoría por una causa relacionada con el ahorro.']]],
 ['q3','Quieres saber si un recordatorio aumenta aportes voluntarios. Si puedes experimentar, ¿qué comparación es más defendible?',[['a','Recordatorio sólo a quienes más aportan.'],['b','Asignar elegibles al azar a recordatorio/control y definir resultado/horizonte antes.'],['c','Comparar este mes con el anterior sin control.']]],
 ['q4','Una política tiene ATE positivo, pero un segmento tiene IC que cruza cero y otro efecto negativo. ¿Qué decisión es más defendible?',[['a','Tratar a todos porque el promedio es positivo.'],['b','Tratar sólo al segmento más grande.'],['c','Priorizar efectos positivos creíbles, pedir más evidencia donde hay incertidumbre y evitar daño.']]]
]}

function TransferCheck({stage,onDone}){const[answers,setAnswers]=useState({});const[busy,setBusy]=useState(false);const[done,setDone]=useState(null);const rows=TRANSFER[stage];async function send(){if(Object.keys(answers).length<4)return;setBusy(true);try{const r=await invoke('transfer-check',{stage,answers});setDone(r);onDone?.()}catch(e){alert(e.message)}finally{setBusy(false)}}return <div className="card card-accent"><div className="eyebrow">{stage==='pre'?'PUNTO DE PARTIDA · NO DA PUNTOS':'TRANSFERENCIA FINAL · NO DA PUNTOS'}</div><h2>{stage==='pre'?'Antes de jugar: 4 decisiones rápidas':'¿Puedes usar lo aprendido en un caso nuevo?'}</h2><p>{stage==='pre'?'No buscamos que ya sepas causalidad; esto nos permite medir cuánto cambia el razonamiento.':'Estas preguntas cambian el contexto para medir transferencia, no memoria del ejemplo.'}</p>{done?<div className="notice notice-green">Registrado: {done.score}/4. {stage==='pre'?'Ahora juega sin preocuparte por esta nota.':'Este resultado no cambia el ranking.'}</div>:<>{rows.map(([id,q,opts],i)=><div className="transfer-question" key={id}><h3>{i+1}. {q}</h3>{opts.map(([v,l])=><label className="choice" key={v}><input type="radio" name={`${stage}-${id}`} checked={answers[id]===v} onChange={()=>setAnswers(a=>({...a,[id]:v}))}/>{l}</label>)}</div>)}<button className="btn btn-primary" disabled={busy||Object.keys(answers).length<4} onClick={send}>{busy?'Enviando…':'Enviar respuestas'}</button></>}</div>}

function Join({onJoined}){const[code,setCode]=useState('');const[name,setName]=useState('');const[busy,setBusy]=useState(false);const[err,setErr]=useState('');async function go(){setBusy(true);setErr('');try{const r=await invoke('join-game',{game_code:code.trim().toUpperCase(),display_name:name.trim()});savePlayerToken(r.token);savePlayerGameCode(code);onJoined()}catch(e){setErr(e.message)}finally{setBusy(false)}}return <Layout subtitle="Participante"><div className="grid grid-2"><div className="card card-accent"><div className="eyebrow">ENTRAR A CAUSAL QUEST</div><h2>Aprendes el concepto, usas tu rol y decides con evidencia.</h2><div className="form-row"><label>Código de esta sesión</label><input className="input" autoCapitalize="characters" placeholder="Ej. NEXO-7K4Q" value={code} onChange={e=>setCode(e.target.value.toUpperCase())}/></div><div className="form-row"><label>Nombre visible</label><input className="input" value={name} onChange={e=>setName(e.target.value)}/></div>{err&&<div className="notice notice-red">{err}</div>}<button className="btn btn-primary" disabled={busy||code.trim().length<4||name.trim().length<2} onClick={go}>{busy?'Entrando…':'Entrar'}</button></div><div className="card"><h3>La regla central</h3><p>Todos los equipos reciben las mismas cuatro especialidades y condiciones. El número de equipos se adapta a la asistencia para mantener grupos de 3–4 personas sin inventar un quinto rol.</p><p className="muted-copy">Mientras la lista esté abierta, tu equipo y rol son provisionales. Al iniciar la primera mini-clase quedan congelados.</p></div></div></Layout>}

function useRemoteState(){const[state,setState]=useState(null);const[error,setError]=useState('');async function refresh(){try{const x=await invoke('game-state');setState(x);setError('');return x}catch(e){setError(e.message);return null}}useEffect(()=>{let stop=false,t;const loop=async()=>{await refresh();if(!stop)t=setTimeout(loop,sleepJitter(3300))};loop();return()=>{stop=true;clearTimeout(t)}},[]);return{state,error,refresh}}

function Round1({state,onSubmit}){const existing=state.decision?.selected||[];const[selected,setSelected]=useState(existing);useEffect(()=>setSelected(existing),[JSON.stringify(existing)]);const customers=state.customers||[];const toggle=id=>setSelected(v=>v.includes(id)?v.filter(x=>x!==id):v.length<10?[...v,id]:v);return <><div className="notice notice-gold">Elige exactamente 10 clientes. Un score alto dice quién probablemente convertirá; todavía no dice quién cambiará por la intervención.</div><div className="customer-grid" style={{marginTop:14}}>{customers.map(c=><button type="button" className={`customer ${selected.includes(c.id)?'selected':''}`} key={c.id} onClick={()=>!state.locked&&toggle(c.id)}><div className="check">{selected.includes(c.id)&&<Check size={14}/>}</div><h4>{c.name}</h4><div className="seg">{c.segment}</div><div className="score-badge">{Math.round(Number(c.score)*100)}% <small>score</small></div></button>)}</div><div className="section-title"><p>{selected.length}/10 seleccionados</p><button className="btn btn-primary" disabled={state.locked||selected.length!==10} onClick={()=>onSubmit({selected})}><Lock size={16}/>{state.locked?'Decisión bloqueada':'Bloquear decisión'}</button></div></>}

const REASONS=[['baseline_difference','Los grupos ya eran distintos antes de la llamada.'],['model_quality','El problema principal es el desempeño del modelo.'],['sample_size','El problema principal es el tamaño de muestra.'],['outcome_timing','El problema principal es que el pago ocurre después.']]
function Round2({state,onSubmit}){const existing=state.decision||{};const[d,setD]=useState({recommendation:existing.recommendation||'',confounder:existing.confounder||'',reason_code:existing.reason_code||''});return <><div className="grid grid-2"><div className="card"><div className="metric"><span>Llamados</span><strong>20%</strong><small>pagan a 30 días</small></div><div className="metric" style={{marginTop:16}}><span>No llamados</span><strong>35%</strong><small>pagan a 30 días</small></div></div><div className="card"><h3>1. ¿Qué harían?</h3>{[['cancel','Cancelar'],['keep','Mantener tal cual'],['redesign','Rediseñar antes de concluir']].map(([v,l])=><label className="choice" key={v}><input type="radio" disabled={state.locked} checked={d.recommendation===v} onChange={()=>setD({...d,recommendation:v})}/>{l}</label>)}</div></div><div className="grid grid-2" style={{marginTop:14}}><div className="card"><h3>2. Variable que amenaza la comparación</h3><select className="select" disabled={state.locked} value={d.confounder} onChange={e=>setD({...d,confounder:e.target.value})}><option value="">Selecciona…</option><option value="edad">Edad</option><option value="mora_previa">Mora previa</option><option value="nombre">Nombre</option></select></div><div className="card"><h3>3. ¿Por qué?</h3>{REASONS.map(([v,l])=><label className="choice" key={v}><input type="radio" disabled={state.locked} checked={d.reason_code===v} onChange={()=>setD({...d,reason_code:v})}/>{l}</label>)}</div></div><div className="section-title"><span/><button className="btn btn-primary" disabled={state.locked||!d.recommendation||!d.confounder||!d.reason_code} onClick={()=>onSubmit(d)}>{state.locked?'Decisión bloqueada':'Bloquear decisión'}</button></div></>}

function Round3({state,onSubmit}){const existing=state.decision&&Object.keys(state.decision).length?state.decision:null;const[d,setD]=useState(existing||{assignment:'',outcome:'',horizon:''});const ready=d.assignment&&d.outcome&&d.horizon;return <><div className="notice notice-gold">Nada viene resuelto: antes de ejecutar deben fijar <b>asignación, resultado y horizonte</b>.</div><div className="grid grid-3" style={{marginTop:14}}>{[['advisor','El asesor decide','Criterio humano.'],['model','ORÁCULO decide','Scores más altos.'],['random','Asignar al azar','Intervención vs control.']].map(([v,t,p])=><button type="button" className={`card ${d.assignment===v?'card-accent':''}`} key={v} disabled={state.locked} onClick={()=>setD({...d,assignment:v})}><h3>{t}</h3><p>{p}</p></button>)}<div className="card" style={{gridColumn:'1/-1'}}><div className="grid grid-2"><div className="form-row"><label>Resultado que importa</label><select className="select" disabled={state.locked} value={d.outcome} onChange={e=>setD({...d,outcome:e.target.value})}><option value="">Selecciona…</option><option value="click">Clic inmediato</option><option value="pago_30d">Pago</option></select></div><div className="form-row"><label>Horizonte</label><select className="select" disabled={state.locked} value={d.horizon} onChange={e=>setD({...d,horizon:e.target.value})}><option value="">Selecciona…</option><option value="1">1 día</option><option value="30">30 días</option><option value="90">90 días</option></select></div></div><button className="btn btn-primary" disabled={state.locked||!ready} onClick={()=>onSubmit({...d,horizon:Number(d.horizon)})}>{state.locked?'Diseño bloqueado':'Bloquear diseño'}</button></div></div></>}

function Round4({state,onSubmit}){const segments=state.segments||[];const capacity=Number(state.policy?.capacity||15000);const initial=()=>{if(state.decision&&Object.keys(state.decision).length){const a={};(state.decision.treat||[]).forEach(x=>a[x]='treat');(state.decision.avoid||[]).forEach(x=>a[x]='avoid');(state.decision.observe||[]).forEach(x=>a[x]='observe');return a}return Object.fromEntries(segments.map(s=>[s.id,'observe']))};const[actions,setActions]=useState(initial);useEffect(()=>{if(segments.length&&!Object.keys(actions).length)setActions(Object.fromEntries(segments.map(s=>[s.id,'observe'])))},[segments.length]);const used=segments.filter(s=>actions[s.id]==='treat').reduce((a,s)=>a+Number(s.audience||0),0);const over=used>capacity;function net(s){return Math.round(Number(s.audience||0)*Number(s.effect||0)/100*Number(s.value_per_result||0)-Number(s.audience||0)*Number(s.unit_cost||0))}function send(){onSubmit({treat:segments.filter(s=>actions[s.id]==='treat').map(s=>s.id),avoid:segments.filter(s=>actions[s.id]==='avoid').map(s=>s.id),observe:segments.filter(s=>actions[s.id]==='observe').map(s=>s.id)})}return <><div className={`notice ${over?'notice-red':'notice-gold'}`}><b>Capacidad: {used.toLocaleString()} / {capacity.toLocaleString()}</b> clientes. Un IC que cruza 0 significa que la dirección del efecto todavía es incierta.</div><div className="card" style={{marginTop:14}}>{segments.map(s=>{const uncertain=Number(s.ci_low)<=0&&Number(s.ci_high)>=0;return <div className="policy-row-v6" key={s.id}><div><b>{s.name}</b><small>{Number(s.audience||0).toLocaleString()} clientes · costo/u {s.unit_cost}</small></div><div><b>{Number(s.effect)>0?'+':''}{s.effect} pp</b><small>IC95% [{s.ci_low}, {s.ci_high}] {uncertain?'· incierto':''}</small></div><div><b>Valor neto est.</b><small>{net(s).toLocaleString()}</small></div><select className="select" disabled={state.locked} value={actions[s.id]||'observe'} onChange={e=>setActions({...actions,[s.id]:e.target.value})}><option value="treat">Intervenir</option><option value="avoid">No intervenir</option><option value="observe">Más evidencia</option></select></div>})}</div><div className="section-title"><span>{over&&'Reduce población intervenida antes de bloquear.'}</span><button className="btn btn-primary" disabled={state.locked||segments.length!==5||over} onClick={send}>{state.locked?'Política bloqueada':'Bloquear política'}</button></div></>}

function Reveal({state}){const r=state.game.round;if(r===1){const rows=state.reveal_payload||[];const observed=rows.length?Math.round(100*rows.reduce((a,c)=>a+Number(c.p1),0)/rows.length):0;const impact=rows.length?Math.round(100*rows.reduce((a,c)=>a+Number(c.effect),0)):0;return <div className="reveal-stage"><div className="eyebrow">PREDICCIÓN VS CAMBIO</div><div className="giant">{observed}%</div><p>conversión esperada entre seleccionados</p><div className="giant" style={{color:'var(--cyan)'}}>+{impact}</div><p>impacto incremental acumulado esperado</p></div>}if(r===2)return <div className="reveal-stage"><div className="eyebrow">ASOCIACIÓN</div><div className="giant" style={{color:'var(--red)'}}>{state.result?.naive_effect_pp??'−15'} pp</div><p>parecía perjudicial</p><div className="eyebrow" style={{marginTop:24}}>EFECTO DEL SIMULADOR</div><div className="giant" style={{color:'var(--green)'}}>+{state.result?.causal_effect_pp??5} pp</div></div>;if(r===3)return <div className="reveal-stage"><div className="split-future"><div className="future"><h3>Tratamiento</h3><strong>{state.result?.treatment??31}%</strong></div><div className="vs">−</div><div className="future"><h3>Control</h3><strong>{state.result?.control??25}%</strong></div></div><div className="giant" style={{marginTop:24,color:'var(--cyan)'}}>+{state.result?.ate_pp??6} pp</div><p>Efecto promedio de una comparación diseñada antes de observar el resultado.</p></div>;return <div className="reveal-stage"><div className="eyebrow">UNA POLÍTICA ≠ UN SIGNO</div><div className="giant">Capacidad + incertidumbre + valor</div><p>Un efecto promedio positivo no obliga a intervenir a todos.</p></div>}

function Microcheck({state,refresh}){const q=MICROCHECKS[state.game.round];const[ans,setAns]=useState('');const[done,setDone]=useState(null);async function send(){const r=await invoke('submit-check',{round:state.game.round,answer:ans});setDone(r.correct);await refresh()}return <div className="card card-accent"><div className="eyebrow">MICROCHECK · NO DA PUNTOS</div><h2>{q.prompt}</h2>{q.options.map(o=><label className="choice" key={o}><input type="radio" checked={ans===o} onChange={()=>setAns(o)}/>{o}</label>)}{done!==null?<div className={`notice ${done?'notice-green':'notice-red'}`} style={{marginTop:14}}>{done?'Correcto.':'Revísalo en el debrief.'}</div>:<button className="btn btn-primary" style={{marginTop:14}} disabled={!ans} onClick={send}>Enviar</button>}</div>}

function Game({onExit}){const{state,error,refresh}=useRemoteState();const[busy,setBusy]=useState(false);if(error&&!state)return <Layout subtitle="Participante"><div className="notice notice-red">{error}</div></Layout>;if(!state)return <Layout subtitle="Participante"><div className="card">Conectando…</div></Layout>;const copy=ROUND_COPY[state.game.round];const team=state.teams.find(t=>t.id===state.player.team_id);const waiting=['lobby','briefing'].includes(state.game.phase),provisional=waiting&&!state.game.roster_locked;async function submit(payload){setBusy(true);try{await invoke('submit-decision',{round:state.game.round,payload,idempotency_key:keyFor(state.game.round,state.player.team_id)});await refresh()}catch(e){alert(e.message)}finally{setBusy(false)}}let body;if(waiting&&!state.transfer?.pre_done)body=<><TransferCheck stage="pre" onDone={refresh}/>{provisional&&<div className="notice notice-gold"><b>Equipo y rol provisionales.</b> La app sigue equilibrando la asistencia. Quedarán fijos cuando el facilitador inicie la primera mini-clase.</div>}</>;else if(waiting)body=<div className="card card-accent"><h2>Listo para comenzar</h2><p>Tu medición inicial quedó registrada. Espera la mini-clase.</p>{provisional&&<div className="notice notice-gold"><b>Equipo y rol provisionales.</b> Pueden cambiar mientras siguen llegando personas. No perderás tu identidad ni tu pretest.</div>}</div>;else if(state.game.phase==='lesson')body=<LessonPanel round={state.game.round} role={state.player.role_code}/>;else if(state.game.phase==='round')body=<><RoleLab state={state}/><div className="section-title"><h2>Decisión del equipo</h2><p>Compartan perspectivas; luego bloqueen una sola decisión.</p></div>{state.game.round===1?<Round1 state={state} onSubmit={submit}/>:state.game.round===2?<Round2 state={state} onSubmit={submit}/>:state.game.round===3?<Round3 state={state} onSubmit={submit}/>:<Round4 state={state} onSubmit={submit}/>}</>;else if(state.game.phase==='closed')body=<div className="card"><h2>Decisiones cerradas</h2><p>Regresen a la sala principal para la revelación.</p></div>;else if(state.game.phase==='reveal')body=<><Reveal state={state}/><div className="card"><Leaderboard state={state}/></div></>;else if(state.game.phase==='teaching')body=<div className="card card-accent"><div className="eyebrow">DEBRIEF</div><h2>{copy.teaching.title}</h2><p style={{fontSize:20}}>{copy.teaching.body}</p></div>;else if(state.game.phase==='microcheck')body=<Microcheck state={state} refresh={refresh}/>;else if(state.game.phase==='finished'&&!state.transfer?.post_done)body=<><TransferCheck stage="post" onDone={refresh}/><div className="card"><Leaderboard state={state}/></div>;else if(state.game.phase==='finished')body=<><div className="reveal-stage"><Sparkles size={40}/><div className="giant">¿A quién podemos cambiar?</div><p>Terminaste la simulación y la medición de transferencia.</p></div><div className="card"><Leaderboard state={state}/></div></>;else body=<div className="card">Partida pausada.</div>;return <Layout subtitle="Participante" actions={<><span className="pill">Equipo <strong>{team?.name}</strong>{provisional?' · provisional':''}</span><span className="pill">Rol <strong>{roleLabel(state.player.role_code)}</strong>{provisional?' · provisional':''}</span><button className="btn btn-ghost" onClick={()=>{if(confirm('¿Salir de este participante? Tu identidad queda guardada en este navegador. Si vuelves desde aquí, recuperarás tu asiento; después de cerrar la lista conservarás exactamente equipo y rol.'))onExit()}}><LogOut size={15}/>Salir</button></>}><div className="round-header"><div><div className="eyebrow">{copy.kicker}</div><h2>{copy.title}</h2><div className="mission">{copy.mission}</div></div><Timer closesAt={state.game.closes_at?new Date(state.game.closes_at).getTime():null} serverNow={state.server_time}/></div>{error&&<div className="notice notice-red">Reconexión: {error}</div>}{busy&&<div className="notice">Enviando decisión…</div>}{body}</Layout>}

export default function SecurePlayV6(){const[logged,setLogged]=useState(()=>Boolean(getPlayerToken()));function exit(){clearPlayerSession();setLogged(false)}return logged?<Game onExit={exit}/>:<Join onJoined={()=>setLogged(true)}/>}
