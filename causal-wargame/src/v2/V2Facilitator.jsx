import React,{useEffect,useState}from'react'
import{ExternalLink,Play,RefreshCw,RotateCcw,Unlock,Users}from'lucide-react'
import{bootstrapGame,invoke,saveFacilitatorToken,saveFacilitatorGameCode,getFacilitatorToken,clearFacilitatorSession}from'../lib/api'
import{V2_ROUNDS,statusCopy}from'./content'

const makeCode=()=>`DF2-${crypto.randomUUID().replaceAll('-','').slice(0,6).toUpperCase()}`
const jitter=n=>Math.round(n*(.85+Math.random()*.3))

function Login({onLogin}){
  const[code,setCode]=useState(''),[pin,setPin]=useState(''),[master,setMaster]=useState('')
  const[showCreate,setShowCreate]=useState(false),[busy,setBusy]=useState(false)
  const[loginErr,setLoginErr]=useState(''),[createErr,setCreateErr]=useState('')
  async function login(){setBusy(true);setLoginErr('');try{const r=await invoke('facilitator-login',{game_code:code.trim().toUpperCase(),pin},'facilitator');saveFacilitatorToken(r.token);saveFacilitatorGameCode(code);onLogin()}catch(e){setLoginErr(e.message)}finally{setBusy(false)}}
  async function create(){setBusy(true);setCreateErr('');try{const c=code.trim().toUpperCase()||makeCode();if(!pin||pin.length<8)throw new Error('Define un PIN de al menos 8 caracteres');if(!master.trim())throw new Error('Escribe la clave maestra para autorizar la creación');await bootstrapGame({game_code:c,pin,bootstrap_secret:master});const r=await invoke('facilitator-login',{game_code:c,pin},'facilitator');saveFacilitatorToken(r.token);saveFacilitatorGameCode(c);onLogin()}catch(e){setCreateErr(e.message)}finally{setBusy(false)}}
  return <main className="v2-shell v2-center"><section className="v2-join-card"><div className="v2-brand">DOS FUTUROS <span>V2</span></div><div className="v2-kicker">FACILITADOR · VERSIÓN SIMPLE</div><h1>Una decisión por ronda. Sin roles, sin mercado.</h1><p className="v2-lead">Cada persona recibe una parte distinta del caso, envía su propuesta y después el equipo decide.</p><div className="v2-form-grid"><label>Código<input value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="DF2-ABC123"/></label><label>PIN del facilitador<input type="password" value={pin} onChange={e=>setPin(e.target.value)} placeholder="8+ caracteres"/></label></div>{loginErr&&<div className="v2-alert error">{loginErr}</div>}<div className="v2-fac-actions"><button className="v2-primary" disabled={busy||code.length<4||pin.length<8} onClick={login}>Entrar</button><button className="v2-secondary" onClick={()=>{setShowCreate(v=>!v);if(!code)setCode(makeCode())}}>Crear nueva sesión V2</button></div>{showCreate&&<div className="v2-panel" style={{marginTop:18}}><h2>Nueva sesión</h2><p>La clave maestra sólo autoriza la creación. El PIN es el que usarás después para entrar.</p><label>Clave maestra<input type="password" value={master} onChange={e=>setMaster(e.target.value)} placeholder="Clave de administración"/></label>{createErr&&<div className="v2-alert error">{createErr}</div>}<button className="v2-primary" disabled={busy||code.length<4||pin.length<8||!master.trim()} onClick={create}>Crear y entrar automáticamente</button></div>}</section></main>
}

function useFacState(enabled){const[state,setState]=useState(null),[error,setError]=useState('');async function refresh(){try{const x=await invoke('v2-facilitator',{action:'state'},'facilitator');setState(x);setError('');return x}catch(e){setError(e.message);return null}}useEffect(()=>{if(!enabled)return;let stop=false,t;const loop=async()=>{await refresh();if(!stop)t=setTimeout(loop,jitter(2200))};loop();return()=>{stop=true;clearTimeout(t)}},[enabled]);return{state,error,refresh}}

function TeamCard({team,players}){const members=players.filter(p=>p.team_id===team.id);const ready=team.humans>0&&team.submitted>=team.humans;return <div className="v2-fac-team"><div style={{display:'flex',justifyContent:'space-between',gap:10}}><b>{team.name}</b><span>{team.decision?'✓ decisión':`${team.submitted}/${team.humans} propuestas`}</span></div><div className="v2-team-roster" style={{justifyContent:'flex-start',margin:'8px 0'}}>{members.map(p=><div className={p.online?'ready':'waiting'} key={p.id}><span>{p.online?'●':'○'}</span>{p.name}</div>)}</div>{ready&&!team.decision&&<small>Todos enviaron: deberían estar deliberando.</small>}</div>}

export default function V2Facilitator(){const[logged,setLogged]=useState(Boolean(getFacilitatorToken()));const{state,error,refresh}=useFacState(logged);const[busy,setBusy]=useState(false);async function act(action,seconds){setBusy(true);try{await invoke('v2-facilitator',{action,seconds},'facilitator');await refresh()}catch(e){alert(e.message)}finally{setBusy(false)}}function exit(){clearFacilitatorSession();setLogged(false);location.reload()}if(!logged)return <Login onLogin={()=>setLogged(true)}/>;if(!state)return <main className="v2-shell v2-center">{error?<div className="v2-alert error">{error}</div>:<div className="v2-loader">Cargando sesión V2…</div>}</main>;const g=state.game,r=V2_ROUNDS[g.round];const base=location.href.replace(/v2-facilitator\.html.*$/,'');const play=`${base}v2-play.html?game=${encodeURIComponent(g.code)}`,wall=`${base}v2-wall.html?game=${encodeURIComponent(g.code)}`,sim=`${base}v2-simulator.html`;return <main className="v2-shell"><div className="v2-fac"><div className="v2-wall-top"><div><div className="v2-brand">DOS FUTUROS <span>V2</span></div><div className="v2-kicker">CONSOLA SIMPLE · {statusCopy(g.status)}</div></div><div className="v2-wall-code"><span>CÓDIGO</span><strong>{g.code}</strong></div></div><div className="v2-fac-grid" style={{marginTop:20}}><section className="v2-panel"><div className="v2-kicker">CONTROL DE CLASE</div><h2>{r?.title||'Partida finalizada'}</h2><p>{r?.case}</p>{r?.evidence&&<div className="v2-alert hint"><span><b>Herramientas visuales:</b> {r.evidence}</span></div>}<div className="v2-fac-actions">{['lobby','briefing'].includes(g.status)&&<button className="v2-primary" disabled={busy||state.humans<1} onClick={()=>act('start')}><Play size={17}/> Iniciar ronda {g.round}</button>}{g.status==='round'&&<button className="v2-primary" disabled={busy} onClick={()=>act('reveal')}><Unlock size={17}/> Cerrar y revelar</button>}{g.status==='paused'&&<button className="v2-primary" disabled={busy} onClick={()=>act('resume')}><Play size={17}/> Reanudar</button>}{g.status==='round'&&<button className="v2-secondary" disabled={busy} onClick={()=>act('pause')}>Pausar</button>}{['reveal','teaching'].includes(g.status)&&<button className="v2-primary" disabled={busy} onClick={()=>act('next')}><Play size={17}/> {g.round>=4?'Finalizar':'Preparar ronda siguiente'}</button>}<button className="v2-secondary" disabled={busy} onClick={()=>act('rebalance')}><RefreshCw size={16}/> Rebalancear lobby</button><button className="v2-secondary" disabled={busy} onClick={()=>{if(confirm('¿Reiniciar esta sesión V2? Se borran jugadores y decisiones.'))act('reset')}}><RotateCcw size={16}/> Reiniciar</button></div><div className="v2-links"><a href={play} target="_blank" rel="noreferrer">Participante V2 <ExternalLink size={13}/></a><a href={wall} target="_blank" rel="noreferrer">Wall V2 <ExternalLink size={13}/></a><a href={sim} target="_blank" rel="noreferrer">Simulador docente V2 <ExternalLink size={13}/></a></div></section><section className="v2-panel"><div className="v2-kicker">LOBBY / PROGRESO</div><h2><Users size={20}/> {state.humans} participantes</h2><p>En V2 no existen roles. Los equipos se forman automáticamente y todos tienen el mismo poder.</p>{state.teams.map(t=><TeamCard key={t.id} team={t} players={state.players}/>)}</section></div><button className="v2-secondary" style={{marginTop:18}} onClick={exit}>Cerrar sesión del facilitador</button></div></main>}
