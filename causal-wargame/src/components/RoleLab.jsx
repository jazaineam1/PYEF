import React,{useEffect,useMemo,useRef,useState}from'react'
import{ROLE_TASKS,ROLE_TOOLS}from'../learning'
import{DAG_SCENARIOS,DECISION_OPTIONS,ECONML_PROVENANCE,ESTIMATORS,EXPERIMENT_BASE,TOOL_GUIDE,UPLIFT_CLIENTS}from'../causal-tools'
import{QuestMarket}from'./QuestMarket'
import{RoleReveal}from'./RoleReveal'
import{TeamContribution}from'./TeamContribution'

const clamp=(x,a,b)=>Math.max(a,Math.min(b,x))
const pp=x=>`${Number(x)>=0?'+':''}${(Number(x)*100).toFixed(1)} pp`
const ROLE_EMOJIS={business:'🦁',data:'🦉',context:'🐈‍⬛',integrator:'🐢',risk:'🦅'}

function FourBlocks({role,evidence}){const g=TOOL_GUIDE[role];return <div className="causal-four"><div><b>1 · Pregunta</b><span>{g.question}</span></div><div><b>2 · Evidencia</b><span>{evidence?.summary||'Explora la herramienta para producir una pieza de evidencia.'}</span></div><div><b>3 · Supuesto</b><span>{evidence?.assumption||g.assumption}</span></div><div><b>4 · Decisión</b><span>{evidence?.decision||g.decision}</span></div></div>}

function DecisionCanvas({round,onEvidence}){const[d,setD]=useState({population:'',treatment:'',comparator:'',outcome:'',horizon:'',estimand:''});const complete=Object.values(d).every(Boolean);const warnings=[];if(d.comparator==='Sin comparador')warnings.push('Sin comparador no queda definido el contraste causal.');if(d.outcome==='Clic inmediato')warnings.push('El clic puede ser un proxy: confirma que corresponde al resultado de negocio.');if(d.population==='Sólo clientes tratados históricamente')warnings.push('La población objetivo puede no coincidir con la población observada/entrenada.');useEffect(()=>{onEvidence({summary:complete?`${d.population} · ${d.treatment} vs ${d.comparator} · ${d.outcome} a ${d.horizon}`:'Pregunta causal todavía incompleta.',assumption:warnings[0]||'Población, tratamiento, comparador y outcome deben ser coherentes y medibles.',decision:complete?`Estimando propuesto: ${d.estimand}.`:'No cerrar decisión hasta completar el canvas.',details:d})},[JSON.stringify(d)]);const field=(key,label)=><label className="form-row"><b>{label}</b><select className="select" value={d[key]} onChange={e=>setD({...d,[key]:e.target.value})}><option value="">Selecciona…</option>{DECISION_OPTIONS[key].map(x=><option key={x}>{x}</option>)}</select></label>;return <div className="tool-body"><div className="decision-canvas-v7">{field('population','Población')}{field('treatment','Intervención')}{field('comparator','Comparador')}{field('outcome','Outcome')}{field('horizon','Horizonte')}{field('estimand','Estimando')}</div>{warnings.map(w=><div className="notice notice-gold" key={w}>⚠️ {w}</div>)}<div className={`notice ${complete?'notice-green':'notice-gold'}`}>{complete?'Pregunta cerrada. Ahora contrástala con Modelos, DAG, Experimentos y Riesgo.':'Tu rol no es secretario: la decisión no está causalmente definida hasta cerrar los seis campos.'}</div></div>}

const practice=[{name:'A',score:.91,cate:.01},{name:'B',score:.66,cate:.28},{name:'C',score:.72,cate:-.03},{name:'D',score:.52,cate:.17}]
function Scatter({rows,estimator,selected,onSelect}){return <svg className="uplift-scatter" viewBox="0 0 620 330" role="img" aria-label="Score predictivo frente a efecto estimado"><line x1="55" y1="275" x2="600" y2="275"/><line x1="55" y1="30" x2="55" y2="275"/><line className="zero" x1="55" y1="235" x2="600" y2="235"/><text x="250" y="318">score predictivo →</text><text x="8" y="28">CATE</text>{rows.map(r=>{const v=Number(r[estimator]);const x=55+Number(r.score)*530;const y=275-clamp((v+.06)/.38,0,1)*235;return <g key={r.id} className={selected===r.id?'selected-point':''} onClick={()=>onSelect(r.id)}><circle cx={x} cy={y} r={selected===r.id?9:6}/><title>{r.name}: score {Math.round(r.score*100)}%, CATE {pp(v)}</title></g>})}</svg>}

function ModelExplorer({round,onEvidence}){const[estimator,setEstimator]=useState('drlearner');const[selected,setSelected]=useState('C02');const[answer,setAnswer]=useState('');if(round===1){const[top]=[...practice].sort((a,b)=>b.score-a.score);const[impact]=[...practice].sort((a,b)=>b.cate-a.cate);useEffect(()=>onEvidence({summary:`En el sandbox, mayor score (${top.name}) no coincide con mayor cambio incremental (${impact.name}).`,assumption:'Para hablar de efecto necesitamos comparar futuros bajo intervención y no intervención.',decision:'No priorizar sólo por P(Y|X); pedir evidencia de impacto incremental.'}),[]);return <div className="tool-body"><div className="mini-model-table"><b>Cliente</b><b>P(Y=1)</b><b>Cambio hipotético</b>{practice.map(r=><React.Fragment key={r.name}><span>{r.name}</span><span>{Math.round(r.score*100)}%</span><span>{pp(r.cate)}</span></React.Fragment>)}</div><div className="notice">Sandbox didáctico, no revela el efecto oculto de los 24 clientes de ORÁCULO. Sirve para descubrir que score alto ≠ efecto alto.</div></div>}
if(round===2){useEffect(()=>onEvidence({summary:'Crudo −12.0 pp; ajuste por variables previas +3.9 pp; IPW +4.2 pp.',assumption:'El ajuste sólo es causal si las variables elegidas bloquean la confusión sin controlar descendientes problemáticos.',decision:'No cancelar la llamada usando sólo la diferencia cruda.'}),[]);return <div className="tool-body"><div className="model-method-grid"><div><b>Comparación cruda</b><strong>−12.0 pp</strong></div><div><b>Regresión ajustada</b><strong>+3.9 pp</strong></div><div><b>IPW</b><strong>+4.2 pp</strong></div></div><div className="notice">Que dos métodos ajustados se parezcan no arregla una mala identificación. Pregunta al Analista Causal qué debe ajustarse y por qué.</div></div>}
if(round===3){useEffect(()=>onEvidence({summary:'El score puede segmentar, pero la asignación determina si la comparación identifica el efecto.',assumption:'Modelo predictivo y diseño experimental responden preguntas diferentes.',decision:'Usar el modelo como apoyo de segmentación, no como sustituto del control aleatorio.'}),[]);return <div className="tool-body"><div className="compare-panels"><div><b>Modelo predictivo</b><span>Ordena P(Y|X).</span></div><div><b>Experimento</b><span>Construye una comparación para estimar ΔY por T.</span></div></div><div className="notice">Pide al Líder de Experimentos efecto + intervalo; tu AUC no responde esa pregunta.</div></div>}
const rows=UPLIFT_CLIENTS;const current=rows.find(r=>r.id===selected)||rows[0];const key=estimator;const top=[...rows].sort((a,b)=>b[key]-a[key]).slice(0,5);const avgSpread=rows.reduce((a,r)=>a+(Math.max(r.tlearner,r.drlearner,r.forest)-Math.min(r.tlearner,r.drlearner,r.forest)),0)/rows.length;useEffect(()=>onEvidence({summary:`${ESTIMATORS[estimator].label}: ${top[0].name} lidera con ${pp(top[0][key])}; ${current.name} tiene score ${Math.round(current.score*100)}% y ${pp(current[key])}.`,assumption:'La concordancia entre estimadores no sustituye la identificación; todos pueden compartir el mismo sesgo.',decision:`Priorizar perfiles con CATE positivo robusto; dispersión media entre estimadores ${(avgSpread*100).toFixed(1)} pp.`,details:{estimator,top:top.map(x=>x.id),selected}}),[estimator,selected]);return <div className="tool-body"><div className="econml-provenance"><span>🦉</span><div><b>Evidencia precomputada con {ECONML_PROVENANCE.engine||'EconML'}</b><small>{ECONML_PROVENANCE.verified?`Verificada · ${ECONML_PROVENANCE.train_rows?.toLocaleString()} filas sintéticas · ${rows.length} perfiles`:'Modo de respaldo didáctico'}</small></div></div><div className="fac-toolbar">{Object.entries(ESTIMATORS).map(([k,m])=><button className={`btn ${estimator===k?'btn-primary':''}`} key={k} onClick={()=>setEstimator(k)}>{m.label}</button>)}</div><Scatter rows={rows} estimator={key} selected={selected} onSelect={setSelected}/><div className="model-readout"><div><small>Seleccionado</small><b>{current.name}</b><span>{current.segment}</span></div><div><small>Score</small><b>{Math.round(current.score*100)}%</b></div><div><small>{ESTIMATORS[estimator].label}</small><b>{pp(current[key])}</b></div></div><div className="top-cate"><b>Top 5 por efecto estimado</b>{top.map((r,i)=><span key={r.id}>{i+1}. {r.name} · score {Math.round(r.score*100)}% · {pp(r[key])}</span>)}</div><div className="causal-question"><b>Si T-Learner, DR-Learner y CausalForestDML coinciden, ¿el resultado se vuelve causal automáticamente?</b><div className="fac-toolbar"><button className="btn" onClick={()=>setAnswer('yes')}>Sí</button><button className="btn" onClick={()=>setAnswer('no')}>No</button></div>{answer&&<div className={`notice ${answer==='no'?'notice-green':'notice-red'}`}>{answer==='no'?'Correcto: la identificación viene antes del estimador.':'No. Tres estimadores pueden concordar y estar sesgados por la misma estructura de datos.'}</div>}</div><details><summary>Ver metodología</summary><p>Estos {rows.length} perfiles se generaron fuera de la clase con datos bancarios sintéticos y estimadores reales de EconML ({ECONML_PROVENANCE.econml_version||'versión registrada'}): T-Learner, DR-Learner y CausalForestDML. El navegador sólo explora el JSON precomputado; no entrena modelos en vivo y no recibe el contrafactual verdadero individual. Que los métodos coincidan no demuestra identificación causal.</p></details></div>}

function initialPos(round,nodes){const maps={2:[[16,20],[30,67],[76,58]],3:[[12,48],[46,20],[80,48]],4:[[10,20],[12,74],[50,45],[82,70]]};return Object.fromEntries(nodes.map((n,i)=>[n,maps[round]?.[i]||[15+i*20,50]]))}
function hasEdge(edges,a,b){return edges.some(e=>e[0]===a&&e[1]===b)}
function DagLab({round,onEvidence}){const scenario=DAG_SCENARIOS[round]||DAG_SCENARIOS[2];const[pos,setPos]=useState(()=>initialPos(round,scenario.nodes));const[edges,setEdges]=useState([]);const[adjust,setAdjust]=useState([]);const[src,setSrc]=useState('');const[dst,setDst]=useState('');const[diagnosed,setDiagnosed]=useState(false);const canvas=useRef(null);useEffect(()=>{setPos(initialPos(round,scenario.nodes));setEdges([]);setAdjust([]);setDiagnosed(false)},[round]);function add(){if(src&&dst&&src!==dst&&!hasEdge(edges,src,dst)){setEdges([...edges,[src,dst]]);setDiagnosed(false)}}function move(id,e){if(!canvas.current)return;const r=canvas.current.getBoundingClientRect();setPos(p=>({...p,[id]:[clamp((e.clientX-r.left)/r.width*100,5,90),clamp((e.clientY-r.top)/r.height*100,8,88)]}))}const allExpected=scenario.expected.every(([a,b])=>hasEdge(edges,a,b));let diagnostic='Construye el grafo y pulsa Diagnosticar.';let cls='notice-gold';if(diagnosed){if(!allExpected){diagnostic='La hipótesis todavía omite al menos una relación necesaria para evaluar el sesgo del escenario.';cls='notice-gold'}else if(scenario.danger==='backdoor'&&!adjust.includes('Mora previa')){diagnostic='🔴 Backdoor abierto: Llamada ← Mora previa → Pago.';cls='notice-red'}else if(scenario.danger==='backdoor'&&adjust.includes('Mora previa')){diagnostic='🟢 El camino de confusión queda bloqueado al condicionar por Mora previa.';cls='notice-green'}else if(scenario.danger==='mediator'&&adjust.includes('Satisfacción')){diagnostic='⚠️ Satisfacción ocurre después de la llamada: controlarla cambia el estimando y bloquea parte del efecto total.';cls='notice-red'}else if(scenario.danger==='mediator'){diagnostic='🟢 Para efecto total, no necesitas controlar el mediador Satisfacción.';cls='notice-green'}else if(scenario.danger==='collider'&&adjust.includes('Uso app')){diagnostic='⚠️ Uso app es collider en Llamada → Uso app ← Motivación. Condicionarlo puede abrir una asociación no causal.';cls='notice-red'}else{diagnostic='🟢 Evitar condicionar el collider mantiene cerrada esa ruta espuria.';cls='notice-green'}}useEffect(()=>onEvidence({summary:diagnosed?diagnostic.replace(/^[^A-Za-zÁÉÍÓÚÑ]+/,''):'DAG aún no diagnosticado.',assumption:'El grafo es una hipótesis causal: debe justificarse con temporalidad y conocimiento del proceso.',decision:scenario.danger==='backdoor'?'Ajustar sólo causas previas necesarias para bloquear backdoors.':scenario.danger==='mediator'?'Definir si se busca efecto total o directo antes de controlar mediadores.':'No ajustar automáticamente por colliders.',details:{edges,adjust}}),[diagnostic,JSON.stringify(edges),JSON.stringify(adjust)]);return <div className="tool-body"><div className="notice">{scenario.prompt}</div><div className="dag-builder" ref={canvas}>{<svg viewBox="0 0 100 100" preserveAspectRatio="none">{edges.map(([a,b],i)=>{const A=pos[a],B=pos[b];return <line key={`${a}-${b}-${i}`} x1={A[0]} y1={A[1]} x2={B[0]} y2={B[1]}/>} )}</svg>}{scenario.nodes.map(n=><button key={n} className={`dag-node-v7 ${adjust.includes(n)?'adjusted':''}`} style={{left:`${pos[n][0]}%`,top:`${pos[n][1]}%`}} onPointerDown={e=>{e.currentTarget.setPointerCapture(e.pointerId)}} onPointerMove={e=>{if(e.currentTarget.hasPointerCapture(e.pointerId))move(n,e)}} onPointerUp={e=>{try{e.currentTarget.releasePointerCapture(e.pointerId)}catch{}}}>{n}</button>)}</div><div className="dag-controls"><select className="select" value={src} onChange={e=>setSrc(e.target.value)}><option value="">Origen…</option>{scenario.nodes.map(n=><option key={n}>{n}</option>)}</select><span>→</span><select className="select" value={dst} onChange={e=>setDst(e.target.value)}><option value="">Destino…</option>{scenario.nodes.map(n=><option key={n}>{n}</option>)}</select><button className="btn" onClick={add}>Añadir flecha</button></div>{edges.length>0&&<div className="edge-chips">{edges.map(([a,b],i)=><button key={`${a}-${b}-${i}`} onClick={()=>{setEdges(edges.filter((_,j)=>j!==i));setDiagnosed(false)}}>{a} → {b} ×</button>)}</div>}<div className="adjust-row"><b>Marcar como variable de ajuste:</b>{scenario.nodes.filter(n=>!['Llamada','Pago'].includes(n)).map(n=><label key={n}><input type="checkbox" checked={adjust.includes(n)} onChange={()=>{setAdjust(v=>v.includes(n)?v.filter(x=>x!==n):[...v,n]);setDiagnosed(false)}}/>{n}</label>)}</div><button className="btn btn-primary" onClick={()=>setDiagnosed(true)}>Diagnosticar estructura</button><div className={`notice ${cls}`}>{diagnostic}</div></div>}

function CausalWarmup({onEvidence}){useEffect(()=>onEvidence({summary:'Todavía no hay DAG: primero formula el contrafactual relevante.',assumption:'Una comparación causal exige razonar sobre el resultado bajo intervención y no intervención.',decision:'Pedir qué tendría que ser comparable antes de interpretar diferencias observadas.'}),[]);return <div className="tool-body"><div className="locked-tool"><b>DAG Lab se desbloquea en la ronda 2.</b><p>Tu trabajo ahora es formular el contrafactual: ¿qué habría pasado con los mismos clientes sin intervención?</p></div></div>}

function ExperimentLab({round,onEvidence}){const[n,setN]=useState(2000);const[assignment,setAssignment]=useState('random');const[outcome,setOutcome]=useState('');const[horizon,setHorizon]=useState('');const t=EXPERIMENT_BASE.treatmentRate,c=EXPERIMENT_BASE.controlRate,diff=t-c;const se=Math.sqrt((t/100*(1-t/100))/(n/2)+(c/100*(1-c/100))/(n/2))*100;const lo=diff-1.96*se,hi=diff+1.96*se;const balance=assignment==='random'?{age:.3,mora:1.2}:assignment==='model'?{age:7.4,mora:28.0}:{age:5.6,mora:21.0};useEffect(()=>onEvidence({summary:`${assignment==='random'?'Azar':assignment==='model'?'Modelo':'Asesor'} · N=${n.toLocaleString()} · ATE ${diff.toFixed(1)} pp · IC95% [${lo.toFixed(1)}, ${hi.toFixed(1)}].`,assumption:assignment==='random'?'La asignación aleatoria sostiene la comparabilidad en expectativa.':'La asignación depende de un criterio que puede volver incomparables los grupos.',decision:outcome&&horizon?`Medir ${outcome} a ${horizon}.`:'Falta fijar outcome y horizonte antes de ejecutar.',details:{n,assignment,outcome,horizon,ate:diff,ci:[lo,hi]}}),[n,assignment,outcome,horizon]);return <div className="tool-body"><div className="experiment-controls"><label><b>Asignación</b><select className="select" value={assignment} onChange={e=>setAssignment(e.target.value)}><option value="advisor">Asesor</option><option value="model">Modelo</option><option value="random">Azar</option></select></label><label><b>N</b><select className="select" value={n} onChange={e=>setN(Number(e.target.value))}><option>200</option><option>2000</option><option>20000</option></select></label><label><b>Outcome</b><select className="select" value={outcome} onChange={e=>setOutcome(e.target.value)}><option value="">Selecciona…</option><option>clic</option><option>pago</option><option>mora 90d</option></select></label><label><b>Horizonte</b><select className="select" value={horizon} onChange={e=>setHorizon(e.target.value)}><option value="">Selecciona…</option><option>1 día</option><option>30 días</option><option>90 días</option></select></label></div><div className="experiment-result"><div><small>Tratamiento</small><b>{t.toFixed(1)}%</b></div><div><small>Control</small><b>{c.toFixed(1)}%</b></div><div><small>ATE</small><b>+{diff.toFixed(1)} pp</b></div><div><small>IC95%</small><b>[{lo.toFixed(1)}, {hi.toFixed(1)}]</b></div></div><div className="balance-panel"><b>Chequeo de balance simulado</b><span>Diferencia edad media: {balance.age.toFixed(1)} años</span><span>Diferencia mora alta: {balance.mora.toFixed(1)} pp</span></div><div className={`notice ${assignment==='random'?'notice-green':'notice-gold'}`}>{assignment==='random'?'El azar no garantiza balance perfecto en cada muestra, pero evita una regla sistemática de selección.':'La simulación muestra desequilibrio porque la asignación usa información del perfil.'}</div></div>}

function ExperimentWarmup({onEvidence}){useEffect(()=>onEvidence({summary:'Todavía no se ejecuta un RCT: evalúa contra qué grupo se compara la intervención.',assumption:'Una diferencia sólo es causal si el comparador representa un contrafactual defendible.',decision:'No prometer causalidad experimental antes de definir la regla de asignación.'}),[]);return <div className="tool-body"><div className="locked-tool"><b>Diseñador de Experimentos se desbloquea en la ronda 3.</b><p>Hasta entonces, pregunta quién decide el tratamiento y por qué los grupos serían comparables.</p></div></div>}

const riskRank=x=>{const s=String(x||'').toLowerCase();return s.includes('high')||s.includes('alto')?3:s.includes('med')?2:1}
function PolicySimulator({state,onEvidence}){const segments=state.segments||[];const baseCapacity=Number(state.policy?.capacity||15000);const[capacity,setCapacity]=useState(baseCapacity);const[budget,setBudget]=useState(120000);const[tolerance,setTolerance]=useState(2);const[selected,setSelected]=useState([]);useEffect(()=>setCapacity(baseCapacity),[baseCapacity]);const rows=segments.map(s=>{const n=Number(s.audience||0),effect=Number(s.effect||0),value=Number(s.value_per_result||0),cost=Number(s.unit_cost||0);return{...s,n,net:n*effect/100*value-n*cost,totalCost:n*cost}});const chosen=rows.filter(r=>selected.includes(r.id));const used=chosen.reduce((a,r)=>a+r.n,0),spend=chosen.reduce((a,r)=>a+r.totalCost,0),net=chosen.reduce((a,r)=>a+r.net,0);const riskViolation=chosen.some(r=>riskRank(r.risk)>tolerance);const valid=used<=capacity&&spend<=budget&&!riskViolation;useEffect(()=>onEvidence({summary:selected.length?`${selected.length} segmentos · ${used.toLocaleString()} personas · valor neto ${Math.round(net).toLocaleString()}.`:'Política aún sin segmentos seleccionados.',assumption:'Los CATE y sus intervalos son utilizables sólo si la identificación previa es defendible.',decision:valid&&selected.length?'Política factible bajo capacidad, presupuesto y tolerancia de riesgo.':'Revisar restricciones antes de recomendar despliegue.',details:{selected,used,spend,net,capacity,budget,tolerance}}),[JSON.stringify(selected),capacity,budget,tolerance]);return <div className="tool-body"><div className="policy-controls-v7"><label><b>Capacidad máxima</b><input className="input" type="number" value={capacity} onChange={e=>setCapacity(Math.max(0,Number(e.target.value)||0))}/></label><label><b>Presupuesto</b><input className="input" type="number" value={budget} onChange={e=>setBudget(Math.max(0,Number(e.target.value)||0))}/></label><label><b>Tolerancia riesgo</b><select className="select" value={tolerance} onChange={e=>setTolerance(Number(e.target.value))}><option value="1">Baja</option><option value="2">Media</option><option value="3">Alta</option></select></label></div><div className="policy-table-v7"><b>Usar</b><b>Segmento</b><b>CATE / IC95%</b><b>Personas</b><b>Valor neto</b><b>Riesgo</b>{rows.map(r=><React.Fragment key={r.id}><input type="checkbox" checked={selected.includes(r.id)} onChange={()=>setSelected(v=>v.includes(r.id)?v.filter(x=>x!==r.id):[...v,r.id])}/><span>{r.name}</span><span>{Number(r.effect)>0?'+':''}{r.effect} pp · [{r.ci_low},{r.ci_high}]</span><span>{r.n.toLocaleString()}</span><span>{Math.round(r.net).toLocaleString()}</span><span>{r.risk}</span></React.Fragment>)}</div><div className={`notice ${valid?'notice-green':'notice-red'}`}>Capacidad {used.toLocaleString()}/{capacity.toLocaleString()} · presupuesto {Math.round(spend).toLocaleString()}/{budget.toLocaleString()} · neto {Math.round(net).toLocaleString()}{riskViolation?' · riesgo excede tolerancia':''}</div><div className="notice">La herramienta calcula factibilidad y valor esperado; <b>no decide por ti</b>. Un IC que cruza cero puede justificar “más evidencia”.</div></div>}

function RiskWarmup({onEvidence}){useEffect(()=>onEvidence({summary:'Aún no hay política segmentada: revisar desperdicio, daño y reversibilidad.',assumption:'Valor esperado no es el único criterio de decisión.',decision:'Preferir acciones reversibles cuando la evidencia es débil.'}),[]);return <div className="tool-body"><div className="risk-checklist"><div><b>1</b><span>¿Podemos desperdiciar presupuesto?</span></div><div><b>2</b><span>¿Algún grupo puede salir perjudicado?</span></div><div><b>3</b><span>¿La decisión es reversible?</span></div></div></div>}

export function RoleLab({state}){const role=state.player.role_code;const meta=ROLE_TOOLS[role];const round=state.game.round;const[evidence,setEvidence]=useState(null);useEffect(()=>setEvidence(null),[role,round]);const Body=useMemo(()=>{if(role==='business')return DecisionCanvas;if(role==='data')return ModelExplorer;if(role==='context')return round>=2?DagLab:CausalWarmup;if(role==='integrator')return round>=3?ExperimentLab:ExperimentWarmup;return round>=4?PolicySimulator:RiskWarmup},[role,round]);return <><RoleReveal state={state} phase="round"/><div className="tool-card" style={{borderColor:meta.color}}><div className="tool-head"><div><div className="eyebrow"><span className="tool-role-emoji" aria-hidden="true">{ROLE_EMOJIS[role]}</span> WAR ROOM · {meta.label}</div><h3>{meta.tool}</h3><p>{meta.purpose}</p></div><div className="pill">Ronda {round}</div></div><div className="notice" style={{marginBottom:14}}><b>Tu misión:</b> {ROLE_TASKS[round]?.[role]}</div>{state.role_card&&<div className="role-intel"><b>Información que sólo ve tu especialidad</b><p>{state.role_card}</p></div>}<FourBlocks role={role} evidence={evidence}/><Body key={`${role}-${round}`} round={round} state={state} onEvidence={setEvidence}/><div className="tool-share">📣 {ROLE_EMOJIS[role]} Tu herramienta debe producir evidencia para los otros cuatro roles. Compártela abajo antes de bloquear la decisión del equipo.</div></div><TeamContribution state={state} evidence={evidence}/><QuestMarket state={state}/></>}
