import React,{useEffect,useMemo,useState}from'react'
import{CheckCircle2,AlertTriangle,XCircle,RefreshCw,Laptop,Copy,ExternalLink}from'lucide-react'
import{invoke}from'../lib/api'

export function ReadinessPanel(){const[data,setData]=useState(null);const[err,setErr]=useState('');const[busy,setBusy]=useState(false);const[copied,setCopied]=useState(false);async function refresh(){setBusy(true);try{setData(await invoke('readiness-check',{action:'report'},'facilitator'));setErr('')}catch(e){setErr(e.message)}finally{setBusy(false)}}useEffect(()=>{refresh()},[]);const total=Number(data?.total||0);const game=data?.game_code||'FUTUROS26';const readinessUrl=useMemo(()=>{try{const u=new URL('system-check.html',location.href);u.searchParams.set('game',game);return u.toString()}catch{return `system-check.html?game=${game}`}},[game]);const message=`Hola equipo. Mañana tendremos Causal Quest. Antes de la sesión, por favor hagan esta prueba de su computador (2–3 minutos):\n\n${readinessUrl}\n\nUsen el mismo computador y navegador con el que participarán. Recomendado: Chrome o Edge actualizado en Windows; Chrome, Edge o Safari en macOS. No usen celular. Al final escriban su nombre y registren el resultado. Si aparece “NO APTO”, envíen una captura hoy mismo.`;async function copy(){try{await navigator.clipboard.writeText(message);setCopied(true);setTimeout(()=>setCopied(false),1800)}catch{prompt('Copia este mensaje:',message)}}return <div className="card readiness-admin"><div className="section-title" style={{marginTop:0}}><div><div className="eyebrow">PRUEBA 24 HORAS ANTES</div><h3>Preparación de participantes</h3></div><button className="btn btn-ghost" onClick={refresh} disabled={busy}><RefreshCw size={14}/>{busy?'Actualizando…':'Actualizar'}</button></div><div className="readiness-kpis"><div><span>Registrados</span><b>{total}/20</b></div><div className="ok"><CheckCircle2/><span>Aptos</span><b>{data?.pass||0}</b></div><div className="warn"><AlertTriangle/><span>Observaciones</span><b>{data?.warn||0}</b></div><div className="bad"><XCircle/><span>No aptos</span><b>{data?.fail||0}</b></div></div><div className="readiness-send"><div><b>Enlace para enviar un día antes</b><small>{readinessUrl}</small></div><div className="readiness-send-actions"><a className="btn btn-ghost" href={readinessUrl} target="_blank" rel="noreferrer"><ExternalLink size={14}/>Abrir</a><button className="btn btn-primary" onClick={copy}><Copy size={14}/>{copied?'Copiado':'Copiar mensaje'}</button></div></div>{total<20&&<div className="notice notice-gold">Faltan <b>{20-total}</b> participante(s) por registrar la prueba previa.</div>}{total===20&&Number(data?.fail||0)===0&&<div className="notice notice-green"><CheckCircle2 size={17}/> Los 20 participantes registraron la prueba y ninguno quedó como “No apto”.</div>}<div className="readiness-list">{(data?.rows||[]).map((r,i)=><div className={`readiness-person ${r.overall}`} key={`${r.name}-${i}`}><span>{r.overall==='pass'?<CheckCircle2/>:r.overall==='warn'?<AlertTriangle/>:<XCircle/>}</span><div><b>{r.name}</b><small><Laptop size={12}/>{r.os} · {r.browser} · {r.viewport} · {r.backend_ms??'—'} ms</small></div></div>)}</div>{err&&<div className="notice notice-red">{err}</div>}</div>}
