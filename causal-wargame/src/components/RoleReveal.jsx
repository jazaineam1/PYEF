import React from'react'
import{LockKeyhole,Star,Sparkles}from'lucide-react'
import{CAUSAL_CODEX,ROLE_ARCHETYPES,newestCapability,unlockedCodex}from'../codex'

export function RoleReveal({state,role:roleProp,round:roundProp,phase:phaseProp='lesson'}){const role=state?.player?.role_code||roleProp;const round=state?.game?.round||roundProp||1;const phase=state?.game?.phase||phaseProp;const meta=ROLE_ARCHETYPES[role];if(!meta)return null;const unlocked=unlockedCodex(round,role,phase);const newest=newestCapability(round,role);return <div className="quest-reveal"><div className="quest-reveal-head"><div><div className="eyebrow">REVELACIÓN DE CAPACIDADES</div><h2>{meta.emoji} {meta.label}</h2><p>{meta.tagline}</p></div><div className="codex-meter"><span>CAUSAL CODEX</span><strong>{unlocked.length}/151</strong><div><i style={{width:`${Math.max(4,100*unlocked.length/151)}%`}}/></div></div></div><div className="role-deck">{Object.entries(ROLE_ARCHETYPES).map(([code,m])=><div className={`role-card-mini ${code===role?'active':''}`} style={{'--role-accent':m.accent}} key={code}><div className="role-avatar">{m.emoji}</div><b>{m.label}</b><small>{m.family}</small><span>{m.tool}</span></div>)}</div><div className="ability-unlock" style={{'--role-accent':meta.accent}}><Sparkles/><div><div className="eyebrow">NUEVA CAPACIDAD</div><h3>{newest?.name}</h3><p>Tu especialidad evoluciona durante la partida. Usa tu herramienta y comparte el hallazgo con el equipo.</p></div><div className="ability-stars">{[1,2,3].map(x=><Star key={x} size={18} fill={x<=Math.min(3,newest?.tier||1)?'currentColor':'none'}/>)}</div></div><div className="codex-strip">{CAUSAL_CODEX.filter(x=>x.role===role).slice(0,12).map(c=>{const on=unlocked.some(u=>u.id===c.id);return <div key={c.id} className={`codex-chip ${on?'on':'off'}`} title={c.name}>{on?<Star size={13}/>:<LockKeyhole size={13}/>}<span>#{String(c.id).padStart(3,'0')}</span></div>})}</div></div>}
